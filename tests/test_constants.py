"""Test the ``wiki.constants`` module.

Pure-constant module with no behavior of its own; the file and folder
name literals are pinned end-to-end by the suites that consume them
(the core engine suites and the CLI command matrix).
"""

from __future__ import annotations

__all__ = []
